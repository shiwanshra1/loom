// Schema stub only, per milestone-1.md Phase 10's own explicit scope limit —
// the full HR-facing placement workflow (endpoints, UI) is a future scope
// decision, not built in this pass.
export type PlacementStatus = 'applied' | 'interviewing' | 'offered' | 'placed';
