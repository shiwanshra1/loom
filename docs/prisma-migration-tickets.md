# Forge Loom — MongoDB → PostgreSQL/Prisma Migration Tickets

Companion to `docs/postgres-database-design.md` (the schema/ER diagram this executes against). Scoped to exactly what's changing: the persistence layer, local infra, and tests. **Auth (JWT/Redis), BullMQ workers, and MinIO/S3 are untouched and not ticketed** — this is a persistence-layer swap, not the broader Firebase migration that was cancelled (see `docs/archive/`).

**Frontend (`apps/client`) impact:** by design, zero — every `.controller.ts`/`.mapper.ts` boundary is preserved, so the JSON shape each endpoint returns doesn't change, and no `apps/client` source file is expected to need an edit. This is *not* the same guarantee the cancelled Firebase plan could make (that plan changed the Talent Pool cursor format and the auth token flow client-side) — here the risk is narrower but not zero: `Decimal` fields don't automatically round-trip to a JSON `number` the way a Mongoose float did, and there's no compiler error if that conversion is missed. See **Phase 10** below for the frontend verification/regression tickets this risk requires — verification tickets, since no code changes are expected, but nothing here should be marked done without someone actually clicking through the app.

**Conventions:** checkpoint and commit locally after each phase; push to `origin/main` only on explicit request (same cadence every prior phase in `memory.txt` has used). Test files referenced below are confirmed to exist: `apps/server/src/__tests__/{auth,enrollment,attendance,citadel,score}.test.ts`.

---

## Phase 0 — Prisma & infra setup — ✅ DONE (2026-09-07)

- [x] **T0.1** `infra/docker-compose.yml`: **correction during execution** — did NOT replace `mongo:7`; added `postgres:16` alongside it (Mongo still serves every not-yet-migrated module through Phase 8). Host port **5433**, not 5432 — this machine already has an unrelated project's Postgres container bound to 5432. Added `infra/postgres-init/01-create-test-db.sql` (runs once via Postgres's own `docker-entrypoint-initdb.d` convention) to create a second `forgeloom_test` database alongside `forgeloom_dev`, mirroring the existing Mongo test/dev split.
- [x] **T0.2** Added `prisma` (devDependency) + `@prisma/client` (dependency) to `apps/server/package.json`; `mongoose` untouched.
- [x] **T0.3** `apps/server/prisma/schema.prisma` created — full 44-model schema from `docs/postgres-database-design.md` §1, byte-verified via `awk` extraction (no transcription drift).
- [x] **T0.4** `apps/server/src/config/env.ts`: **correction** — added `databaseUrl: required('DATABASE_URL')` but did **not** remove `mongoUri` (see design-doc-level correction below). Removing it now would break every not-yet-migrated module's Mongo connection at boot.
- [x] **T0.5** `apps/server/src/config/prisma.ts` created as specified (`connectPrisma`/`disconnectPrisma` wrappers); wired into `apps/server/src/server.ts` alongside (not instead of) `connectDb()`.
- [x] **T0.6** `apps/server/.env.example` updated. `.env.local` and `.env.test` did not exist at all in this checkout — created both from scratch with dev-only placeholder secrets (JWT/S3 following the established `dev-only-secret-change-me` convention; Razorpay uses an explicit placeholder since no real test-mode key is available in this session — payment flows won't work until real keys are supplied, but nothing in Phase 0/1 touches payments).
- [x] **T0.7** `prisma migrate dev --name init` run against `forgeloom_dev`; `prisma migrate deploy` run against `forgeloom_test`. Both databases confirmed to have all 44 tables via `\dt`.
- **Checkpoint — met**: `prisma generate` succeeded, migration applied cleanly, `npm run typecheck`/`npm run build` both clean across all 3 workspaces with Mongoose still fully in place and the Prisma client also importable.

**Design-doc-level correction found during execution (see `docs/postgres-database-design.md` for the permanent record):** the schema doc's `cuid()` primary-key choice was justified for the *final* Phase 8 data migration, but doing the cutover live, phase by phase, means both databases run side by side for a while — and every Mongoose foreign-key field (`Team.trainerId`, `Enrollment.studentId`, etc.) is a strict `Schema.Types.ObjectId`. A Prisma `cuid()` is not valid ObjectId hex, so a user created after the Phase 1 cutover can never be referenced by a still-Mongo module until that module also migrates (or Phase 8 runs). Confirmed with the user directly (three options presented); chose to **accept this gap as expected dev-stage behavior** rather than switch ID formats or pre-migrate identity data. Phase 1 below implements accordingly, with the gap showing up exactly where predicted (empty `workload`/`programs` for new accounts) and disclosed inline in code comments at each site.

---

## Phase 1 — Identity module group — ✅ DONE (2026-09-07)

Files: `apps/server/src/modules/auth/*.ts` (`auth.service.ts`, `auth.mapper.ts`, `auth.controller.ts`, `profileFactory.ts`, `collegeProvisioning.ts`), `apps/server/src/modules/communityMembers/{communityMember.service.ts,communityMember.mapper.ts}`, `apps/server/src/modules/colleges/{college.service.ts,college.mapper.ts}`, `apps/server/src/modules/hrProfile/{hrProfile.service.ts,hrProfile.mapper.ts}`, `apps/server/src/modules/talentPool/{talentPool.service.ts,talentPool.mapper.ts}`, plus `apps/server/src/scripts/seed.ts` (not originally ticketed for this phase — see below).

- [x] **T1.1 — correction**: did **not** delete any Mongoose model files. `User.ts` and the 11 profile models are referenced by dozens of not-yet-migrated files across every other module (almost everything refs `User`) — deleting them now would break the whole server's compile, not just auth. Actual deletion stays Phase 9's job, once literally nothing imports them. All imports in the 5 migrated modules were swapped to `prisma.*` as ticketed; the old model files simply became unused by these specific files.
- [x] **T1.2** `profileFactory.ts` rewritten exactly as ticketed.
- [x] **T1.3** `communityMember.service.ts`'s `addMember` rewritten to `prisma.communityMember.upsert(...)` on the composite key, exactly as ticketed.
- [x] **T1.4 — partial correction**: `m.assignedStudents.length` → `prisma.studentProfile.count({ where: { mentorId: m.id } })` exactly as ticketed (both sides already Prisma-backed as of this phase). `t.assignedTeams.length` could **not** become `prisma.team.count()` as ticketed — `Team` isn't migrated until Phase 3, so no Team data exists in Postgres yet. Kept as a guarded Mongo `TeamModel.countDocuments({trainerId})` read, skipped entirely (returns 0, not a thrown CastError) when the trainer's id isn't valid Mongo ObjectId hex — i.e. for any trainer registered after this cutover. Revisit this function once Phase 3 lands.
- [x] **T1.5** `talentPool.service.ts` rewritten. The free-text query (name-or-any-skill substring match) has no equivalent in Prisma's type-safe query builder for `String[]` columns — implemented via a fully parameterized `prisma.$queryRaw`/`Prisma.sql` composition (`ILIKE` + `unnest(skills)`) to preserve the original Mongo regex search's behavior exactly, rather than degrading it. Flagged in code as the spot the design doc's future tsvector/pg_trgm upgrade will eventually replace.
- [x] **Unticketed but required**: `apps/server/src/scripts/seed.ts` fully rewritten to Prisma — it directly calls `profileFactory.ts`/`collegeProvisioning.ts`, which are now Prisma-only, so it could not have kept working otherwise. This is effectively Phase 9's T9.5 done early, but scoped only to this script's existing identity/college seeding (no course/citadel seeding exists in it today).
- [x] **Unticketed but required**: `apps/server/src/__tests__/globalSetup.ts` extended with a scoped Postgres truncate (the 15 Phase-1 tables only, hardcoded list) alongside the existing Mongo drop — a deliberately narrow preview of Phase 7's real generalized version. `apps/server/src/__tests__/auth.test.ts` updated to suspend/reinstate via `prisma.user.update` instead of `UserModel.updateOne` (the account it manipulates now lives in Postgres).
- [x] **Disclosed regression, expected and documented**: `attendance.test.ts` and `enrollment.test.ts` are now `describe.skip`-ed with an inline explanation. Both create their test user via the shared Mongo-only `createTestUser` helper, then log in through the real HTTP endpoint — which is Postgres-only as of this phase, so that user is never found. Fixing this for real requires Phase 2 (Course/Enrollment/Attendance move to Prisma too, so the same user works everywhere again) — attempting a workaround now would just be re-implementing Phase 2 early. `citadel.test.ts`/`score.test.ts` were unaffected (they never log in via HTTP).
- **Checkpoint — met**: `auth.test.ts` passes (9/9) against Postgres, run twice consecutively to confirm the truncate guardrail. Full suite: 15 passed, 5 skipped (disclosed above), 0 failed. Live end-to-end verified over real HTTP against a running dev server (not just tests): seeded all 16 dev accounts fresh via the Prisma-backed seed script; logged in as `student1`/`college1`/`hr1`/`community1`; confirmed `GET /colleges` lists both real Postgres colleges; confirmed `college1`'s faculty/programs endpoints return correct-but-empty-as-expected data (0 workload, 0 programs — the accepted gap manifesting exactly as predicted, not an error); confirmed HR profile GET/PATCH round-trips a real update; confirmed HR directory joins the right email; confirmed Talent Pool search returns both seeded students with correctly-joined college names; confirmed a Community Leader can invite an existing account and list members back; confirmed a Student gets blocked from `/admin/users`.

---

## Phase 2 — Courses/Enrollment/Sessions module group — ✅ DONE (2026-09-07)

Files: `apps/server/src/modules/courses/*.ts`, `catalog/*.ts`, `enrollments/*.ts`, `sessions/*.ts`, `progress/courseProgress.service.ts`, `videoProgress/*.ts`, `assessments/*.ts`, `certificates/*.ts`.

- [x] **T2.1 — correction, same as Phase 1**: did not delete `Course.ts`/`Enrollment.ts`/etc. — `apps/server/src/modules/admin/analytics.service.ts` (Phase 6) still imports and reads them directly, and would fail to compile otherwise. Deletion stays Phase 9's job. All 8 migrated modules' own imports were swapped to `prisma.*`.
- [x] **T2.2** `course.service.ts` syllabus handling ported exactly as ticketed — `createCourse` uses a nested `syllabus: { create: [...] }` write, `updateCourse`'s syllabus branch does `prisma.syllabusDay.deleteMany` + nested `create` inside a `$transaction`, preserving wholesale-replace-on-update semantics.
- [x] **T2.3** Decimal→number conversions applied: `course.mapper.ts` (`price`), `enrollment.mapper.ts` (`course.price`, `paymentAmount`), and at the Razorpay call site in `enrollment.service.ts` (`course.price.toNumber()` before `Math.round(...*100)`). Live-verified over real HTTP: a course created with price `499.50` round-trips as the JSON number `499.5`, not a string or object — the exact regression this ticket existed to prevent.
- [x] **T2.4** `session.service.ts`'s `markAttendance` ported to `prisma.attendanceRecord.upsert({ where: { sessionId_studentId: {...} }, ... })` exactly as ticketed.
- [x] **T2.5** `videoProgress.service.ts` ported to `prisma.videoProgress.upsert({ where: { studentId_courseId_dayNumber: {...} } })` exactly as ticketed.
- [x] **T2.6 — correction**: grepped `11000` across the whole codebase before writing this — there was no existing Mongoose duplicate-key catch anywhere to replace (the ticket assumed one existed; it didn't). `certificate.service.ts` keeps its original pre-check pattern (`findUnique` before `create`) unchanged in shape, just on Prisma now. No `P2002` handling was added since there's nothing here to replace it.
- [x] **Unticketed but required**: `college.service.ts`'s `getCollegePrograms` — previously a disclosed Phase 1 gap (read `StudentProfile` from Prisma but `Enrollment`/`Course` from Mongo, so new-era students always showed 0 programs) — rewritten to read Enrollment/Course from Prisma too, since both moved this phase. Closes that gap for real.
- [x] **Unticketed but required**: `notification.service.ts`'s `createNotification` gained a guard (`mongoose.isValidObjectId(userId)`, silently skip if false) — Notification isn't migrated until Phase 5, and Phase 2 code (`session.service.ts` on cancel, `certificate.service.ts` on issue) now calls it with Postgres-native student ids that would otherwise throw a Mongoose CastError. Centralized once here rather than guarded at every call site.
- [x] **Unticketed but required**: `apps/server/src/__tests__/helpers.ts` gained `createTestUserPg` (Prisma-backed test user creation) alongside the existing Mongo-backed `createTestUser` — the latter stays for `citadel.test.ts`/`score.test.ts`, which never log in over HTTP and still need Mongo-compatible ids for still-Mongo Team/Sprint documents. `attendance.test.ts` and `enrollment.test.ts` fully rewritten onto `createTestUserPg` + `prisma.*` calls throughout and un-skipped.
- **Checkpoint — met**: `attendance.test.ts` and `enrollment.test.ts` pass against Postgres (previously skipped in Phase 1 for exactly this reason). Full suite: 5 files, 20/20 passing, run twice consecutively. Live end-to-end over real HTTP against a running dev server: created and published a course (`price: 499.50` round-tripped correctly as a number), browsed the catalog, confirmed enrollment creation fails gracefully with a 502 (not a crash) against the placeholder Razorpay key from Phase 0, manually activated an enrollment to test past the payment gate, posted video progress (95% → `completed: true`, confirming the 90% threshold), pulled the course-progress rollup (50%, 1/2 modules), issued a real certificate (real PDF generated and uploaded to MinIO, real presigned download URL, real public verify endpoint returning correct data), and confirmed re-issuing against the now-`completed` enrollment is correctly rejected.

---

## Phase 3 — Citadel module group — ✅ DONE (2026-09-07)

Files: `apps/server/src/modules/teams/*.ts`, `sprints/*.ts`, `problemStatements/*.ts`, `cohorts/*.ts` — **plus `apps/server/src/jobs/citadelWorker.ts`, missing from this list originally** (see below).

- [x] **T3.1 — correction, same pattern as Phases 1/2**: did not delete the Mongoose models — nothing outside this phase's own files referenced them, but the deletion is still deferred to Phase 9 for consistency with how every other phase has handled it (a blanket single cleanup pass is safer than staggered partial deletions).
- [x] **T3.2** `memberStudentIds` → `TeamMember` nested-write replace via `deleteMany` + nested `create` under `$transaction`, exactly as ticketed. `TrainerProfile.assignedTeams[]` writes dropped entirely — confirmed no `updateOne`/`$addToSet` calls remain anywhere in `team.service.ts`.
- [x] **T3.3** `problemStatementId` assignment now a real FK (`prisma.problemStatement.findFirst({where:{id, status:'open'}})`); the app-level "must be open" business check preserved as ticketed.
- [x] **T3.4** `tasks[]` → `SprintTask` nested creates with explicit `order`, exactly as ticketed. `replaceTasks` does the same `deleteMany` + nested-create-under-`$transaction` pattern as T3.2's member replace, for the same "whole list replaced on update" reason.
- [x] **T3.5** `deliverables[]` → `ProblemStatementDeliverable` nested creates with `order`; `toggleBookmark`/`expressInterest` ported to `prisma.bookmark.delete`/`create` and `prisma.interestExpression.upsert` on the composite key, exactly as ticketed.
- [x] **T3.6** Mentor feedback append → `prisma.milestoneFeedback.create(...)`, exactly as ticketed — confirmed this removes the parent-document race condition the original comment flagged.
- [x] **Scope gap found and fixed, not originally ticketed**: `jobs/citadelWorker.ts` (`checkInvestorUnlock`) reads/writes `Team`/`Sprint`/`ProblemStatement`/`InvestorAccessGrant` directly — all migrated this phase — but wasn't in Phase 3's file list. Left unmigrated, it would throw a Mongoose CastError the moment a Postgres-native team completed its 3rd sprint (this is exactly what "grants investor access" depends on, so it's not an edge case — it's the phase's own headline feature). Fully ported to Prisma; `jobs/citadelQueue.ts` needed no changes (no DB access, just a BullMQ queue definition).
- [x] **Same "accept the gap" pattern surfaced twice more, both disclosed inline in code**: `scoreEvent.service.ts`'s `recordScoreEvent` (called by `sprint.service.ts` on feedback/completion) gained the same Mongoose-ObjectId guard as `notification.service.ts` did in Phase 2 — `ScoreEvent` isn't migrated until Phase 4, so a Postgres-native team member's score event silently doesn't get recorded yet (their `builderScore` won't reflect it until Phase 4 lands). Also discovered, but deliberately **not** fixed this phase: `jobs/scoreWorker.ts`'s `recomputeBuilderScore` still writes to Mongo's `StudentProfileModel` even though `StudentProfile` moved to Postgres back in Phase 1 — a latent bug from that phase, currently masked because nothing exercised the score-write path until this phase's sprint completions started calling it. Fixing the write target now would require rewriting `score.test.ts` (currently fully Mongo-based) ahead of its own Phase 4 turn, so it's left as-is and flagged here explicitly for Phase 4 to pick up alongside its own `ScoreEvent` migration — not silently carried forward unmentioned.
- **Checkpoint — met**: `citadel.test.ts` rewritten fully onto Prisma (`createTestUserPg`, real `StudentProfile`/`Team`/`Sprint`/`ProblemStatement` rows) and passes against Postgres. One assertion had to be dropped, disclosed inline in the test file: the original asserted a Mongo `Notification` document was created for the unlocked team, which no longer happens for a Postgres-native recipient (the guard above) — the grant/problem-statement-closed assertions still fully cover the worker's actual behavior. Full suite: 5 files, 20/20 passing, run twice consecutively. **Live end-to-end over real HTTP**, not just the automated suite: created a real problem statement as `forge_admin`, created a team as `college_admin` with real `trainer1`/`mentor1` assigned, added `student1` as a member and assigned the problem statement (confirmed all 3 sprint cycles materialized with correct dates); ran the full submit→feedback→complete cycle three times as the actual `student1`/`mentor1` accounts, confirming each `complete` auto-activated the next cycle; after the 3rd completion, confirmed via the API that `investorAccessGranted` flipped to `true` and the problem statement's status flipped to `closed` — proving the BullMQ worker genuinely ran end-to-end against Postgres, not just that the enqueue call didn't throw. Also confirmed bookmark toggle (on/off) and interest-expression for an unrelated student, and confirmed cohort creation + forward-only phase advancement (bootcamp→activation correctly rejected with 400).

---

## Phase 4 — Scoring module group — ✅ DONE (2026-09-07)

Files: `apps/server/src/modules/scoring/scoreEvent.service.ts`, `apps/server/src/modules/sprints/sprint.service.ts` (the write site — no changes needed here, it already called `recordScoreEvent` with plain string literals that pass through unchanged), the BullMQ score-recompute worker (`apps/server/src/jobs/scoreWorker.ts`).

- [x] **T4.1 — done, plus the Phase 1 fix flagged in Phase 3**: `scoreEvent.service.ts`'s `recordScoreEvent` ported to `prisma.scoreEvent.create(...)` — the Mongoose-ObjectId guard added in Phase 3 was **removed**, not just left in place, since Postgres has no CastError-on-format-mismatch failure mode the way Mongoose did (an FK that doesn't exist fails with a clear `P2003` instead, and every caller already passes a real student id). `jobs/scoreWorker.ts`'s `recomputeBuilderScore` uses `prisma.scoreEvent.groupBy({by:['category'], where:{studentId}, _sum:{points:true}, _avg:{points:true}})` — both `_sum` (events/project/team, capped at 100) and `_avg` (mentor, averaged not summed) computed in one aggregation query rather than fetching every row. **Also fixed the write target**: `recomputeBuilderScore` now writes `builderScore` via `prisma.studentProfile.update(...)` instead of Mongo's `StudentProfileModel` — this was flagged in Phase 3 as a latent Phase 1 bug (StudentProfile moved to Postgres then, but nothing had exercised the score-write path yet to expose it) and fixed here as planned, alongside `ScoreEvent`'s own migration since the two are inseparable (no correct write target without a real event log to compute from).
- [x] **T4.2** Confirmed — `scoreQueue.ts`'s job payload (`{studentId: string}`) needed zero changes; only `scoreWorker.ts`'s internal DB calls changed.
- **Checkpoint — met**: `score.test.ts` rewritten onto `createTestUserPg` + `prisma.studentProfile`/`prisma.scoreEvent` calls throughout — all 4 cases pass, including the exact historical Phase 8 hand-computed value (43) and the mentor-average-not-sum/category-cap behaviors. Full suite: 5 files, 20/20 passing, run twice consecutively. **Live end-to-end over real HTTP, not just the unit test**: created a *fresh* problem statement/team/sprint cycle (the Phase 3 live-test data never actually recorded any score events, since the Mongoose guard was silently skipping them back then — confirmed baseline `builderScore: 0` for both seeded students via Talent Pool search first), ran a real student through submit→feedback(5★)→complete as `student2`/`mentor2`, and confirmed via the Talent Pool endpoint that `builderScore` landed at exactly **43** — proving the real BullMQ worker recomputed it correctly against Postgres end-to-end, not just that the isolated unit test's math was right.

---

## Phase 5 — Engagement module group — ✅ DONE (2026-09-07)

Files: `apps/server/src/modules/bookings/*.ts`, `notifications/*.ts`, `community/*.ts`, `events/*.ts`, `accessRequests/*.ts`, `speakerTopics/*.ts`.

- [x] **T5.1 — correction, same pattern as every prior phase**: did not delete the Mongoose models — deferred to Phase 9.
- [x] **T5.2** `EventRegistration` upsert ported to `prisma.eventRegistration.upsert({ where: { eventId_userId: {...} } })` exactly as ticketed.
- [x] **T5.3** `AccessRequest` upsert ported to `prisma.accessRequest.upsert(...)` on the composite key; `findById` → `prisma.accessRequest.findUnique({where:{id}})` — the surrogate id was preserved in the schema exactly as the design doc called for, so this needed no rework.
- [x] **T5.4** `notification.service.ts` ported to plain `create`/`findMany`/`updateMany`, exactly as ticketed. **The Mongoose-ObjectId guard added in Phase 2 (and relied on by Phases 3 and 4) was removed** — this is the phase that makes it unnecessary, not just another place to reapply it.
- [x] **T5.5** `listMyBookings`'s `$or` ported to a native Prisma `OR: [{requesterId}, {mentorId}]` in one query, exactly as ticketed — confirmed no query-splitting was needed, unlike what the cancelled Firestore plan would have required.
- **Checkpoint — met**: no dedicated `.test.ts` file exists for this domain (pre-existing gap, not introduced by this migration) — verified via a full **live HTTP smoke test** instead, per the ticket's own instruction: created a booking (student→mentor1), confirmed the recipient's notification genuinely arrived (previously silently skipped by the now-removed guard, going back to Phase 2's own live test), marked it read, cancelled the booking and confirmed the *other* party got the cancellation notification, confirmed `listMyBookings` correctly returns the booking from both sides of the `OR`; posted a community post and confirmed it appears in the feed for a different user; hosted an event and registered for it as a different user, confirmed `registeredCount`/`isRegistered` update correctly; requested access to that event as a Media Partner, confirmed the request is idempotent on a duplicate call (same id returned, not a duplicate row) and that the host received a notification for it (twice, once per call — matching the original Mongo behavior's own unconditional-notify-on-every-call semantics, not a regression), confirmed the host can approve it and a non-host gets 403 trying to; proposed and listed a speaker topic.

---

## Phase 6 — Admin/Dormant + remaining models — ✅ DONE (2026-09-07) — last functional phase

Files: `apps/server/src/modules/admin/*.ts`.

- [x] **T6.1 — correction, same pattern as every prior phase**: did not delete `Placement.ts` — deferred to Phase 9. Reconfirmed it's still fully dormant (only self-referenced in its own model file, zero controllers/services touch it).
- [x] **T6.2** All of `admin.service.ts` (national stats, user listing, status updates) and `analytics.service.ts` (course completion rate, score distribution, attendance trend, Citadel funnel) ported to Prisma exactly as ticketed — every single model this phase touches (`User`, `College`, `StudentProfile`, `Enrollment`, `AttendanceRecord`, `InterestExpression`, `Team`, `Sprint`, `InvestorAccessGrant`) was already migrated in Phases 1-5, so this was a clean, self-contained swap with no new cross-domain gaps — the first phase where that's been true. `InterestExpressionModel.distinct('userId')` → `prisma.interestExpression.findMany({distinct:['userId'], select:{userId:true}})`, exactly as the ticket suggested.
- **Checkpoint — met, and this is the headline result of the whole phase**: live curl smoke-test of all 4 admin endpoints against Postgres. `GET /admin/national-stats` returned **real, correct counts reflecting actual accumulated activity from every prior phase's live testing** (`totalColleges:2, totalStudents:2, venturesLaunched:1`) — before this phase, this endpoint had been silently reading a Mongo collection that received zero writes since Phase 1's cutover, so it would have shown stale zeros indefinitely no matter how much real Postgres activity happened. `GET /admin/analytics` returned a score distribution and Citadel funnel that **exactly matched the live test data accumulated across Phases 2-5** (`scoreDistribution` showing one student at 0 and one at the Phase 4 test's 43; `citadelFunnel: {interested:1, teamsFormed:2, sprintsCompleted:4, investorGranted:1}` — precisely the two teams and four completed sprint cycles created across Phase 3's and Phase 4's live tests). `GET /admin/users` correctly joined college names across both seeded college clusters. `PATCH /admin/users/:id/status` genuinely suspended `student1` (confirmed real login blocking with a 403) and reinstated them (confirmed login works again) — a real functional test, not just a status-field check.
- **This closes the demo-readiness gap flagged to the user at the start of Phase 4**: every backend module is now on Prisma; no "accept the gap" guard is silently skipping anything for a current account anywhere in the app. Phases 7-10 (test harness generalization, historical data migration, cleanup, frontend verification) don't add or fix functionality — a fresh account can now click through all 11 roles' features without hitting a known dead end.

---

## Phase 7 — Test harness rewrite — ✅ DONE (2026-09-07)

Files: `apps/server/vitest.config.ts`, `apps/server/src/__tests__/globalSetup.ts`, `apps/server/src/__tests__/helpers.ts`.

- [x] **T7.1 — done, and more fully than ticketed**: before touching this, ran a full grep audit (`grep -rl mongoose`, `grep -rl "from '../models"`) across all of `apps/server/src` to confirm it was actually safe — found that after Phase 6, **zero live application code depends on Mongo at all** except `config/db.ts` itself (still called at server boot, a Phase 9 concern, out of scope here). On that basis, removed the Mongo `dropDatabase()` step entirely (not just swapped its guardrail) and replaced the Phase 1-5 hardcoded `MIGRATED_TABLES` array with a generic `information_schema.tables` enumeration excluding `_prisma_migrations` — this file never needs editing again as future phases land.
- [x] **T7.2** Kept `fileParallelism:false`/`singleFork:true` exactly as ticketed; rewrote the comment to explain the real current reason (one shared Postgres database with a single truncate-at-start, not per-file isolation) instead of the stale "shared Mongoose connection" explanation.
- [x] **T7.3 — done, plus a naming cleanup the ticket implied but didn't spell out**: `connectDb`/`disconnectDb` in `helpers.ts` now literally are `connectPrisma`/`disconnectPrisma` (`export const connectDb = connectPrisma`), not separate re-implementations. Since this made the parallel `connectPrisma`/`connectDb` calls in every test file's `beforeAll` redundant, removed the redundant calls from all 5 test files. Also renamed `createTestUserPg` back to plain `createTestUser` across `helpers.ts` and all 4 call sites (`attendance.test.ts`, `enrollment.test.ts`, `citadel.test.ts`, `score.test.ts`) — the "Pg" suffix existed specifically to disambiguate from a Mongo-backed version that no longer exists, so keeping two names for one implementation would have been pure noise. The original Mongo-backed `createTestUser` was deleted outright (confirmed zero remaining callers first) rather than kept as a dead helper.
- [x] **Unticketed but required, found via the same grep audit**: `college.service.ts` still had two Mongo fallbacks left over from Phase 1 (`countTeamsForTrainer` reading `TeamModel`, `listPartnerColleges` reading `CohortModel`) that were never updated once Team and Cohort actually migrated to Prisma in Phase 3 — an oversight that predates this phase but was only caught now. Both fixed to plain `prisma.team.count(...)`/`prisma.cohort.findMany(...)`; this is a genuine, real functional fix (not test-harness-only), confirmed live: `trainer1`'s faculty workload now correctly shows `1` (Team Alpha from Phase 3) instead of the stale `0` it would have shown forever, and `college1`'s partner-directory `activePhase` now correctly shows `"bootcamp"` instead of `null`.
- **Checkpoint — met, with one disclosed caveat**: full suite green across the vast majority of runs. Ran it **15 consecutive times** total during verification (well beyond the ticket's "twice in a row") — 14 passed, 1 failed with an error I wasn't able to capture in full detail (only saw a truncated summary) and could not reproduce despite 10 further consecutive runs in a tight loop immediately after with zero failures. Disclosed honestly rather than either hidden or falsely claimed as "fully resolved": most likely a transient environment hiccup (this machine had many concurrent dev-server/curl sessions running throughout this whole migration effort) rather than a logic bug in the new generic truncate loop, but this is a judgment call based on non-reproduction, not a confirmed root cause — worth a closer look if it recurs.

---

## Phase 8 — One-time Mongo → Postgres data migration script

New file: `apps/server/src/scripts/migrateMongoToPostgres.ts` (sits alongside the existing `apps/server/src/scripts/seed.ts`).

- [x] **T8.1** Script connects to both a live Mongo instance (read-only, via `mongoose` kept as a devDependency-only import for this one script) and the new Prisma client, and migrates table-by-table in FK-dependency order. **Correction during execution**: the order named in this ticket (`Cohort`/`Team`/`TeamMember` before `ProblemStatement`) was backwards — `Team.problemStatementId` is a real FK (a fix `postgres-database-design.md` made for a field Mongo declared without a `ref` at all), so migrating Team first threw `P2003` against a Postgres table that didn't exist yet. Fixed by moving `migrateProblemStatementsAndDeliverables()` immediately after `migrateProfiles()`, before `migrateCohortsTeamsAndMembers()` — ProblemStatement has no dependency on Team, so this reordering is safe. Actual final order: `College` → `User` → all 11 profile tables → `ProblemStatement`/`ProblemStatementDeliverable` → `Cohort`/`Team`/`TeamMember` → `Course`/`SyllabusDay` → `CourseSession` → `Enrollment` → `AttendanceRecord`/`VideoProgress`/`Assessment`/`Certificate` → `Sprint`/`SprintTask` → `MilestoneSubmission`/`MilestoneFeedback` → `InvestorAccessGrant`/`Bookmark`/`InterestExpression` → `ScoreEvent` → `Booking`/`Notification`/`CommunityPost`/`Event`/`EventRegistration`/`AccessRequest`/`SpeakerTopic` → `Placement` (confirmed zero source documents in Mongo and no application code path that ever wrote to it — genuinely dormant, not just unmigrated).
- [x] **T8.2** **Identity preservation**: every row's Postgres `id` is set to the literal `mongoDoc._id.toString()` (24-char hex) via `prisma.<model>.create({ data: { id: mongoDoc._id.toString(), ... } })` — no id-mapping table needed anywhere (design doc §0.1).
- [x] **T8.3** For the 5 normalized embedded arrays, flatten each subdocument into a row referencing the parent's already-migrated id, assigning `order` from the array index for `SprintTask`/`ProblemStatementDeliverable` (design doc §0.5).
- [x] **T8.4** For `TrainerProfile.assignedTeams[]`/`MentorProfile.assignedStudents[]`, the script does nothing — dropped entirely, implicitly correct via `Team.trainerId`/`StudentProfile.mentorId` (design doc §0.3).
- [x] **T8.5** Script is idempotent-safe via `skipDuplicates: true` on `createMany` calls (and try/catch on `P2002` for the per-row `create()` calls needed for nested writes), plus a `--dry-run` flag that logs counts without writing.
- [x] **T8.6 — unticketed, found via execution against the real dev Mongo database.** The design doc's happy-path schema doesn't account for real historical data drift. Three concrete corruptions found and handled, not papered over:
  1. **A field added after some documents were created**: one `CollegeProfile` (the early `collegeadmin1` test account, predating the `collegeId` field's introduction in Phase 6 of `milestone-1.md`) has no `collegeId` at all, which is `NOT NULL` in Postgres. Filtered out with a `console.warn` count rather than crashing or fabricating a value.
  2. **Orphaned User references** — a `Course.trainerId` pointed at a Mongo `ObjectId` (`6a787143aa6d98806e7a0db2`) that does not exist in the current 19-document `users` collection at all, confirmed via direct `mongosh` cross-reference. Traced to a historical dev-fixture reset documented in `memory.txt`'s Phase 5&6 entry ("deleted just those 4 Users + their profile docs... and re-ran the seed script"). This is not a one-off: the same class of dangling reference showed up independently in `Enrollment.studentId` (3 of 8 rows) and `AttendanceRecord`/`VideoProgress` (1 row each) once tested — all tracing to the same deleted historical accounts, confirmed via the same `mongosh` cross-reference method before being trusted as "genuinely orphaned" rather than a bug in the new validation logic.
  3. **Fixed systematically, not per-field-as-discovered**: `migrateUsers()` now returns the *authoritative* set of User ids that actually exist in the Postgres target after migration (queried back from Postgres itself, not assumed from the Mongo count — this also correctly captures ids skipped by `skipDuplicates` on an email collision, which is a second, distinct way a Mongo-valid id can still be invalid in the target). Every downstream function that touches a User-referencing field takes this `validUserIds` set: optional fields (`Course.trainerId`, `CourseSession.trainerId`, `Team.mentorId`/`trainerId`, `ProblemStatement.postedBy`) are nulled out via a shared `sanitizeUserRef()` helper; required fields (`Enrollment.studentId`, `AttendanceRecord.studentId`/`markedBy`, `VideoProgress.studentId`, `Certificate.studentId`, `Booking.requesterId`/`mentorId`, `Notification.userId`, `CommunityPost.authorId`, `Event.hostedBy`, `EventRegistration.userId`, `AccessRequest.requesterId`, `SpeakerTopic.speakerId`, `ScoreEvent.studentId`, `Bookmark`/`InterestExpression.userId`, each `MilestoneFeedback.mentorId` individually within its parent submission, each `CommunityMember.userId` individually within its parent leader) cause that specific record (or sub-record) to be skipped, with a logged before/after count at every step so silent data loss is never actually silent.
- **Checkpoint — met.** Per the user's explicit direction (offered the choice via AskUserQuestion because old Mongo data and the already-seeded, already-live-tested Postgres dev database share emails but not ids): the script is written and fully verified for correctness, but was **never run against `forgeloom_dev` or `forgeloom_test`**. Instead, verified against a throwaway `forgeloom_migration_scratch` database (`prisma migrate deploy` against a fresh empty schema, script run live, not `--dry-run`) — completed end-to-end with zero crashes after the 3 fixes above. Row counts in the scratch database were cross-checked directly against the script's own reported counts for all migrated tables (all matched), and every skip (`CollegeProfile` ×1, `Enrollment` ×3, `AttendanceRecord` ×1, `VideoProgress` ×1) was independently confirmed via raw `mongosh` queries to be a genuine orphaned/missing reference, not an over-eager filter. `forgeloom_migration_scratch` was dropped afterward; `forgeloom_dev`'s `User` count (16) confirmed unchanged before and after this entire exercise. Full verification suite also re-run for this phase: `typecheck`, `build`, and root-level `eslint` on the new script all clean; `npx vitest run` green (5 files / 20 tests) across 2 consecutive runs.

---

## Phase 9 — Cleanup

- [x] **T9.1** `apps/server/package.json`: `mongoose` dependency removed; `npm install --workspace=apps/server` re-run to drop it from `package-lock.json` too (confirmed zero remaining `"mongoose"` entries in the lockfile).
- [x] **T9.2** Deleted `apps/server/src/models/` directory in full (38 files: 34 model files + `index.ts` + a couple of shared type-helper files).
- [x] **T9.3** Deleted `apps/server/src/config/db.ts`.
- [x] **T9.4** `infra/docker-compose.yml`: removed the `mongo` service block and the `mongo_data` volume entirely (this ticket's "should already be done in T0.1" assumption didn't hold — Mongo was still explicitly defined there through Phase 8, load-bearing for every not-yet-migrated module; this is the real removal). Grepped `.gitignore`/`README.md`/every `*.md` for `mongo_data`/`MONGO_URI`/`mongoose` — zero hits outside the migration's own historical docs (`postgres-database-design.md`, `prisma-migration-tickets.md`, the archived Firebase docs), which correctly keep them since they document what happened. The already-running `infra-mongo-1` container from prior phases was left running rather than force-stopped (a live container isn't a file-tree cleanup concern, and it's a one-command `docker stop` whenever wanted) — it's simply no longer part of this compose file's managed services going forward.
- [x] **T9.5** N/A — already done in Phase 1, ahead of this ticket (disclosed there): `scripts/seed.ts` was rewritten to Prisma back then because it called the now-Prisma-only `profileFactory`/`collegeProvisioning` directly.
- [x] **T9.6** N/A — already done ahead of schedule during the original planning pass (2026-09-01); re-verified now that all code phases are actually complete: no residual Mongo language found in either doc.
- [x] **T9.7 — unticketed, found and resolved during execution.** `apps/server/src/scripts/migrateMongoToPostgres.ts` (Phase 8's one-time data migration script) imports all 34 Mongoose models and `mongoose` itself directly — deleting `models/`/`mongoose` breaks its build. This is a genuine tension with Phase 8's own framing ("written properly for future use"), not an oversight to route around silently, so it was put to the user directly rather than assumed: keep mongoose+all 34 model files alive indefinitely just to keep one already-run, already-verified script compiling, or delete it too. **User chose to delete it** — it already did its job (written, verified against a scratch DB, 3 real bugs found and fixed, fully documented in this doc's Phase 8 section and in `memory.txt`), remains fully recoverable from git history (commit `29b7058`) if a real future migration is ever needed, and T9.4 retires the Mongo docker service in this same phase anyway, so there would be no source database left to run it against regardless.
- [x] **T9.8 — unticketed, required to keep the build green after T9.2/T9.3.** `server.ts`'s `connectDb()` call and import removed (was the literal reason `models/`/`db.ts` couldn't be deleted before now); `env.ts`'s `mongoUri`/`MONGO_URI` removed (from `env.ts` itself and from `.env.local`/`.env.test`/`.env.example`); a stale comment in `__tests__/globalSetup.ts` referencing `models/`/`config/db.ts` by path (from Phase 7, before this phase existed) updated to stop naming now-deleted files.
- **Checkpoint — met.** `grep -rn "mongoose" apps/server/src` returns nothing; `npm run typecheck`/`npm run build` clean across all 3 workspaces; `npx eslint apps/server/src apps/client/src` clean (1 pre-existing unrelated warning); `npx vitest run` green (5 files/20 tests), run twice consecutively. **Live-verified, not just typechecked**: the actual running dev server (`tsx watch`) picked up the `db.ts` deletion mid-session — briefly crashed on the stale import exactly as expected until `server.ts`'s edit landed a few seconds later (this is `tsx watch`'s normal file-delete-before-dependent-edit race, not a real bug), then recovered cleanly on its own with zero manual restart. Re-ran the `student1@forgeloom.dev` login smoke test afterward and confirmed a real JWT still comes back, and the client still serves `200` — full stack proven Mongo-free end-to-end, not just by absence of imports.

---

## Phase 10 — Frontend regression verification

No `apps/client` source changes are expected in this migration — every ticket below is a **verification/QA ticket**, not a code-change ticket. Skip a ticket only if you can point to the specific backend change that makes it moot (e.g. if T2.3's `.toNumber()` conversion is done, T10.1 should just pass, not be skipped as "not applicable").

- [x] **T10.1 — Decimal→number regression check — passed.** Verified live over real HTTP (not just reading the client code): logged in as `course_admin1`, `GET /courses/mine` returned `price: 499.5` as a genuine JSON number (`typeof === 'number'`), not a string or `{"s":1,"e":2,"d":[...]}`-shaped serialized `Decimal`. Same course via `GET /catalog` as `student1` — same result. No enrollments existed for `student1` at verification time, so `Enrollment.paymentAmount` itself wasn't exercised this pass (not a failure — nothing to check), but the identical `.toNumber()` conversion pattern noted in T2.3 covers both fields the same way, and `Course.price` passing is strong evidence the pattern works. The 4 client call sites named in this ticket needed zero edits, as predicted.
- [x] **T10.2 — Talent Pool cursor pagination regression check — passed, verified with real data, not just a status code.** Logged in as `hr1`, called `GET /talent-pool?limit=1` (page 1 → Student Two, `score: 43`, real `nextCursor` string), then `GET /talent-pool?limit=1&cursor=<that value>` (page 2 → Student One, `score: 0`, `nextCursor: null`) — a different student on page 2, not a repeat of page 1, and the cursor correctly signals end-of-results after both seeded students were returned. `apps/client/src/features/hr/data.ts` needed zero edits, as predicted.
- [x] **T10.3 — Full click-through smoke test, phase by phase — done as an API-level smoke test, with an explicit gap disclosed below.** No browser-automation tool was available in this session, so this was verified via direct authenticated HTTP calls to the exact endpoints each phase's UI screens call (not literal clicking in a rendered browser) — logged in as all 16 seed accounts across all 11 roles, then hit: Phase 1 (HR Talent Pool directory, Community Leader member list), Phase 2 (Course Admin course list, Trainer teaching list), Phase 3 (Problem Statements list, Mentor team list), Phase 5 (Bookings/Notifications/Community feed/Events, all `/mine` or `/feed` endpoints), Phase 6 (Admin `national-stats` + `analytics`, both returning real non-zero live data: `totalColleges:2, totalStudents:2, venturesLaunched:1`). All 26 checks across every section passed on a clean run. **What this does and doesn't prove**: it confirms every backend contract the frontend depends on still returns `200` with the expected shape post-cleanup (real regression coverage for "did Phase 9's Mongo removal or any earlier phase's refactor silently break a route"), but it is not a substitute for actually rendering each screen in a browser and clicking through — that step is still open and is on the user to do (or ask for again with browser tooling available) before calling the UI itself demo-verified, not just its API surface.
- **Checkpoint — met for T10.1/T10.2 (both are genuine full regression checks); T10.3 met at the API level only, browser-level click-through still outstanding per the disclosure above.** Zero `apps/client` source edits were made or needed for any of the three tickets, confirming the "zero client changes expected" design goal held all the way through.

---

## Cross-cutting items not owned by a single phase

- [ ] **TX.1 — `Decimal` conversion audit.** Beyond the 4 files named in T2.3, grep for any other read site of `Course.price`/`Enrollment.paymentAmount` that assumes a plain JS number (e.g. arithmetic, JSON serialization without `.toNumber()`) before considering Phase 2 complete.
- [ ] **TX.2 — Unique-violation error handling audit.** Every `service.ts` file that currently catches a Mongoose duplicate-key error (code `11000`) needs the Prisma equivalent (`P2002`) — grep `11000` across `apps/server/src/modules/**` to find every site, not just the certificate one called out in T2.6.
- [ ] **TX.3 — Decide `Placement`'s fate explicitly.** It's a dormant, zero-traffic table (per the repo audit, same status the doc `docs/postgres-database-design.md` records). This migration carries the table forward for schema completeness per T6.1's no-op, but confirm with the user whether that's still wanted once the migration is actually underway, versus dropping the model entirely until a future phase builds real placement tracking.
