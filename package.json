{
  "name": "forge-loom",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run build --workspace=packages/shared-types && npm run dev --workspace=apps/server",
    "dev:client": "npm run dev --workspace=apps/client",
    "seed": "npm run build --workspace=packages/shared-types && npm run seed --workspace=apps/server",
    "build": "npm run build --workspace=packages/shared-types && npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run build --workspace=packages/shared-types && npm run test --workspace=apps/server",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "eslint": "^9.14.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.14",
    "globals": "^15.11.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
